<script type="text/javascript">
	if (!window.mmtimestamp) {
		window.mmtimestamp = {
			head: Date.now(),
			previous: Date.now(),
			previousLabel: 'orig',
			log: function (type) {
				var now = Date.now();
				_gaq.push(['_trackEvent', 'CDN Timings Total', type, '', now - window.mmtimestamp.head]);
				window.mmtimestamp.previous = now; 
				window.mmtimestamp.previousLabel = type;
			}
		};
	}
</script>
